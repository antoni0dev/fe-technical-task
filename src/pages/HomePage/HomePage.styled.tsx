import styled from 'styled-components';

export const Wrapper = styled.div`
  min-height: 100%;
  background: var(--color-gradient-bubblegum);
  padding-inline: 16px;
`;

export const Main = styled.main`
  display: flex;
  justify-content: center;
  gap: 24px;
  align-items: flex-start;
  margin-top: 12px;
`;
